# Copie para .env e cole a connection string do seu banco PostgreSQL (Neon).
# No Neon: Dashboard > Connection string (use a opção "Pooled connection").
DATABASE_URL="postgresql://USUARIO:SENHA@HOST.neon.tech/DB?sslmode=require"
